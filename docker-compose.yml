version: '3'
services:
  app:
    env_file: ../.env
    working_dir: /app/src
    volumes:
      - .:/app
    container_name: nodejs
    image: node:alpine
    ports:
      - '${SITE_PORT}:${SITE_PORT}'
    depends_on:
      - db

  db:
    image: mongo
    container_name: ${MONGO_NAME}
    ports:
      - '${MONGO_PORT}:${MONGO_PORT}'
