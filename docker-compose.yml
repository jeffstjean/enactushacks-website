version: '3'
services:
  app:
    env_file: .env
    working_dir: /app/src
    volumes:
      - .:/app
    container_name: ${SITE_NAME}
    image: node:alpine
    ports:
      - '${SITE_PORT}:${SITE_PORT}'
